org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Both flags below only mean anything to AGP 9, and this project is built by
# AGP 8 — see the comment in settings.gradle.kts for why. Deleting them is
# pointless: Flutter's migrator puts them back on the next build. They are
# inert here, so they are left alone.
#
# Do not set builtInKotlin to true while AGP is held at 8: it is what AGP 9
# reads to decide whether to reject modules applying the Kotlin Gradle Plugin,
# and nsd_android still applies one.
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
